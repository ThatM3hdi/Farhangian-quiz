version: '3.8'

services:
  backend:
    build: .
    container_name: quiz_game_backend
    ports:
      - "8081:81"
    volumes:
      - ./app:/code/app
    environment:
      - TZ=Asia/Tehran
    command: uvicorn app.main:app --host 0.0.0.0 --port 81 --reload